# Delivery Engine — Environment Variables
# Copy this to .env.local and add your API keys

# AI Provider (at least one required for live agent responses)
# Without a key, the app runs in demo mode with sample outputs
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
